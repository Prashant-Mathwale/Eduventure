<!DOCTYPE html>
<html lang="en" class="dark">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= t.login %> - <%= t.eduventure %>
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="flex items-center justify-center min-h-screen">
  <div class="card w-full max-w-sm p-8">
    <h1 class="text-3xl font-bold mb-8 text-center text-blue-400">
      <%= t.eduventure %>
        <%= t.login %>
    </h1>
    <form action="/login" method="POST" class="space-y-6">
      <div>
        <!-- Label has been removed for a cleaner UI. The placeholder provides the necessary information. -->
        <input type="text" id="username" name="username" placeholder="<%= t.username %>" required
          class="w-full p-3 border border-gray-700 rounded-lg bg-gray-900 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      <div>
        <!-- Label has been removed for a cleaner UI. The placeholder provides the necessary information. -->
        <input type="password" id="password" name="password" placeholder="<%= t.password %>" required
          class="w-full p-3 border border-gray-700 rounded-lg bg-gray-900 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      <button type="submit" class="w-full btn-primary py-3 text-lg font-bold">
        <%= t.login %>
      </button>
    </form>
    <div class="mt-6 text-center">
      <p class="text-gray-400">New to Eduventure? <a href="/register" class="text-blue-400 hover:underline">Create an
          account</a></p>
    </div>
  </div>
</body>

</html>