<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.teacherDashboard %> - <%= t.eduventure %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-layout">
        <%- include('partials/sidebar') %>
        <main class="main-content">
            <h1 class="text-3xl font-bold mb-2"><%= greeting %>, <%= user.name || user.username %>!</h1>
            <p class="text-foreground/70 mb-6"><%= t.teacherWelcome %></p>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Card for Total Students -->
                <div class="card p-6">
                    <h2 class="text-xl font-semibold text-blue-400 mb-2"><%= t.totalStudents %></h2>
                    <p class="text-4xl font-bold"><%= students.length %></p>
                </div>

                <!-- Card for Total Quizzes -->
                <div class="card p-6">
                    <h2 class="text-xl font-semibold text-blue-400 mb-2"><%= t.totalQuizzes %></h2>
                    <p class="text-4xl font-bold"><%= quizzes.length %></p>
                </div>

                <!-- Quick Actions Card -->
                <div class="card p-6">
                    <h2 class="text-xl font-semibold text-blue-400 mb-4"><%= t.quickActions %></h2>
                    <div class="space-y-3">
                        <a href="/teacher/students/add" class="block text-center w-full btn-primary"><%= t.addStudent %></a>
                        <a href="/teacher/quizzes/new" class="block text-center w-full btn-primary bg-green-600 hover:bg-green-700"><%= t.createQuiz %></a>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>

